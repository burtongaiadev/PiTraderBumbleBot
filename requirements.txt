# PiTrader - Requirements
# Optimisé pour Raspberry Pi 5 (4GB RAM)

# HTTP Client (pour Twelve Data, NewsAPI, Telegram, Ollama)
requests>=2.31.0

# Variables d'environnement
python-dotenv>=1.0.0

# Monitoring système (optionnel mais recommandé)
psutil>=5.9.0

# Données financières (fallback pour indices et fondamentaux)
# Note: yfinance 0.2.40 for Python 3.9 compatibility (3.10+ required for newer versions)
yfinance>=0.2.30,<=0.2.40
